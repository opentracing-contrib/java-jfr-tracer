env:
  global:
    - TIMEOUT=5000
  - ORG_GRADLE_PROJECT_JAVA_8=oraclejdk8
addons:
  hosts:
    - localhost
language: java
install: true
script:
  - ./gradlew build
jdk:
  - oraclejdk8
branches:
  only:
    - feature/add-travis